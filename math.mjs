/**
 * @param  {Number} a
 * @param  {Number} b
 * @return {Number}
 */
const sum = (a, b) => a + b;

/**
 * @param  {Number} a
 * @param  {Number} b
 * @return {Number}
 */
const subtract = (a, b) => a - b;

export { sum, subtract };
